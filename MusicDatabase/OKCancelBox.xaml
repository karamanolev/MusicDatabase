<UserControl x:Class="MusicDatabase.OKCancelBox"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignWidth="400">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        
        <Button Grid.Column="1" Click="btnOK_Click" Margin="2,0" Width="80" IsDefault="True" IsEnabled="{Binding IsOKEnabled}">OK</Button>
        <Button Grid.Column="2" Click="btnCancel_Click" Margin="2,0,0,0" Width="80" IsCancel="True">Cancel</Button>
    </Grid>
</UserControl>
